specVersion: "v1"
serviceId: "textomize"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "textomize-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
